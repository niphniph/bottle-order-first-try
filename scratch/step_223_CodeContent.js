"# Implementation Plan - Rebuild Purely Static Tap-to-Swap (No Transitions or Animations)\n\nThis plan completely deletes all transitions, transforms, animations, drag listeners, scaling effects, and motion effects from all bottles, shelves, boards, screens, and containers. Swapping is handled purely by state sequence updates and instant static rendering, with absolutely zero visual motion.\n\n## Proposed Changes\n\n### 1. Purely Static CSS Lock\nWe will apply the strict CSS lock to BOTH `style.css` and the embedded `<style>` block in `index.html`. This rules out all animations, transitions, transforms, and translates on the game board, screens, shelves, rows, and bottles:\n\n```css\n.game-screen,\n.game-board,\n.top-board,\n.bottle-row,\n.shelf,\n.bottle-slot,\n.bottle {\n  transform: none !important;\n  translate: none !important;\n  animation: none !important;\n  transition: none !important;\n}\n\n.bottle-row {\n  display: flex;\n  align-items: flex-end;\n  justify-content: center;\n}\n\n.bottle-slot {\n  position: relative;\n  transform: none !important;\n}\n\n.bottle {\n  transform: none !important;\n}\n\n/* Selected bottle gets only a visual border/glow, no lift or movement */\n.bottle-slot.selected {\n  outline: 3px solid #ffd84d !important;\n  box-shadow: 0 0 10px rgba(255, 216, 77, 0.8) !important;\n  transform: none !important;\n  animation: none !important;\n  transition: none !important;\n}\n\n.bottle-slot.selected .bottle {\n  transform: none !important;\n}\n```\n\n### 2. Deleting Animating-Swap Transitions\n- We will completely remove `.bottle-slot.animating-swap` and `.bottle.animating-swap` styles from both `style.css` and `index.html`.\n- We will remove `@keyframes fadeIn`, `@keyframes pulseGlow`, and all other hover-lifts or transitional animations.\n\n### 3. Deleting any drag/touch/mouse move listeners\n- We will ensure `initDragAndDrop` is a clean, empty no-op function and does not bind or hook any events.\n\n### 4. Direct Re-rendering on Swap\n- Verify `handleBottleClick` instantly swaps sequ
<truncated 549 bytes>